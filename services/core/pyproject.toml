[project]
name = "moj-asystent-core"
version = "0.1.0"
description = "Local-only FastAPI assistant core and desktop protocol boundary."
readme = "README.md"
authors = [
    { name = "Marcel Bialk", email = "mirekromantik@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "faster-whisper>=1.2.1",
    "httpx>=0.28.1",
    "numpy>=2.5.3",
    "onnx>=1.20.1,<2",
    "openwakeword>=0.4.0",
    "mss>=10.1.0; sys_platform == 'win32'",
    "piper-tts>=1.8.0",
    "pillow>=11.3.0",
    "psutil>=7.2.2",
    "pycaw>=20251023",
    "pywinauto>=0.6.9; sys_platform == 'win32'",
    "pydantic>=2.13.5",
    "scipy>=1.18.1",
    "silero-vad>=6.2.1",
    "sounddevice>=0.5.6",
    "uvicorn>=0.52.4",
    "websockets>=17.1",
    "nvidia-ml-py>=13.610.43",
]

[project.scripts]
moj-asystent-core = "moj_asystent_core.main:main"
moj-asystent-stt-benchmark = "moj_asystent_core.stt_benchmark:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ty.environment]
python-version = "3.12"

[dependency-groups]
dev = [
    "hypothesis>=6.168.0",
    "jsonschema[format]>=4.26.0",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "ruff>=0.16.6",
    "ty>=0.0.79",
]
